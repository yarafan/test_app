<h1> Create new post</h1>

<%= render 'shared/errors_message', :object => @post %>

<%= form_for @post, :url => posts_path, :html => { :multipart => true }  do |f| %>

<%= render partial: 'form', locals: { f:f } %>

<p><%= f.submit "Create post" %> </p>

<% end %>