<h1> Create new user</h1>

<%= render 'shared/errors_message', :object => @user %>

<%= form_for @user do |f| %>

<%= render partial: 'form', locals: { f:f } %>

<p><%= f.submit "Create user" %> </p>

<% end %>